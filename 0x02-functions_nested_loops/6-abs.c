#include "main.h"
#include <stdlib.h>
/**
 *_abs- computes the absolute value of an interger
 *@n: number to be computed
 *Return: returns the computes int.
 */
int _abs(int n)
{
if (n > 0)
return (n);
else
return (-n);
}
