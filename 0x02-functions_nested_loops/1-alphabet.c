#include "main.h"
/**
 * print_alphabet- prints the alphabet, in a lowercase,
 * followed by a new line.
 *Return: returns 0.
 */

void print_alphabet(void)
{
int a;

for (a = 'a'; a <= 'z'; a++)
_putchar(a);

_putchar('\n');
}
