#include "main.h"
/**
 * to lower case
 * writing by romanus
 *Return: return 0
 */

void print_alphabet(void);
int main(void)
{
  int a;
  for(a = '0'; a <= 'z'; a++)
    _putchar(a);
    _putchar('\n');
}
