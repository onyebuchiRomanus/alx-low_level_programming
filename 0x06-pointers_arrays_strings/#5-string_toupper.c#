#include "main.h"
/**
 *string_toupper - chages lowercase to uppercase
 * @n: pointer
 *Return: char
 */
char *cap_string(char *)
{
int i;
i = 0;
while (n[i] != '\0')
{
if (n[ni] >= 'a' && n[i] <= 'z')
{
n[i] = n[i] - 32;
i++;
}
}
return (n);
}